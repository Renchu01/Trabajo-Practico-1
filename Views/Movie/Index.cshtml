@model List<Movie>

<div class="text-center">
    <h1 class="display-4"></h1>
    <p> <h1>Las Mejores peliculas del Mundo</h1> <br>
        por Renzo Pochettino
    </p>
</div>

<ol>
    @foreach (var item in Model)
    {
        <li><a asp-action="Detail" asp-route-id="@item.Code">@item.Name</a></li>
     } 
</ol>

<a asp-action="Create">Agregar nueva Pelicula</a>